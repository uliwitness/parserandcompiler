int foo(int outsideNum) {
    return(700 + outsideNum);
}

int main(int iterations) {
    int x = 0;
    while (x < iterations) {
        x = x + 1;
        printNum(x);
    };

    return(x + foo(77));
}
